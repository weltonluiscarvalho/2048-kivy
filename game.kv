#: import FadeTransition kivy.uix.screenmanager.FadeTransition

ScreenManager:
    id: screen_manager
    transition: FadeTransition(duration=.5)
    MainScreen:
        id: main_screen
        name: "main_screen"
    BoardScreen:
        id: board_screen
        name: "board_screen"
